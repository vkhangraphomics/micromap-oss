"""API route modules.

Submodules (taxa, diseases, metabolites, search, drugs, genes, proteins,
pathways, biomarkers, papers, networks, discovery, provenance, graph) are
imported by name from ``api.main`` — no re-exports from this package's
``__init__`` are needed.
"""
